<nav class="navbar navbar-default navbar-static-top">
    <div class="container">
        <div class="navbar-header">
            <!--<a class="navbar-brand" href="/">
                <span id="vkChar">H</span>
            </a>-->
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar"
                    aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
        </div>
        <div class="navbar-collapse collapse" id="navbar">
            <ul class="nav navbar-nav">
                <li <% if (page === 'grabber') { %>class="active"<% } %>><a href="/grabber">О сайте</a></li>
                <li <% if (page === 'checker') { %>class="active"<% } %>><a href="/checker">Видео</a></li>
                <li <% if (page === 'poster') { %>class="active"<% } %>><a href="/poster">Контакты</a></li>
            </ul>
            <% if (user) { %>

            <ul class="nav navbar-nav navbar-right">
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown"><b><%= user.username %></b> <span
                                class="badge"><%= user.deposit.toFixed(2) %> руб</span> <b class="caret"></b></a>
                    <ul class="dropdown-menu">
                        <li><a href="#"><span class="glyphicon glyphicon-usd" aria-hidden="true"></span> Баланс</a>
                        </li>
                        <li><a href="/settings"><span class="glyphicon glyphicon-cog" aria-hidden="true"></span>
                                Настройки</a>
                        </li>
                        <li><a href="/logout"><span class="glyphicon glyphicon-log-out" aria-hidden="true"></span> Выход</a>
                        </li>
                    </ul>
                </li>
            </ul>
            <% } else { %>

            <form class="navbar-form navbar-right">
                <a href="/login" class="btn btn-primary">Войти</a>
                <a href="/register" class="btn btn-primary">Регистрация</a>
            </form>
            <% } %>
        </div>
    </div>
</nav>